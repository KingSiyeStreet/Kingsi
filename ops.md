# OPS — 2026-08-21

OHLC hop-1: `data-api.binance.vision/api/v3/klines` (~424ms). `fapi.binance.com`=451 **63ms**. `api.binance.us` ETH 4h vol **295** vs Vision **141480** = **480×** — interdit EMA. **TF = bougies closes `klines[:-1]`** ; live = ticker. Ticker ONDO = **ONDOUSDT** (ONDUSDT=400). Funding/OI OKX BTC funding **0.0100%** OI **30484**. Bybit 403.

F&G: GET `https://api.alternative.me/fng/?limit=2` → **72 Greed** (J-1 62). Interdit WebSearch. Interdit limit≠2.

CG: global cap **2.588T** BTC.D **60.00**. `order=percent_change_24h_desc` → BTC/ETH. `top_gainers_losers` 401. Tri client 250 vol≥5e6 : ENA +42.9 XPL +27.9 PUMP +24.1.

PM **piège 21/08** : `markets?q=bitcoin` **et** `markets?q=crypto` ignorent `q` → Xi / Dem 2028.  
Hop-1 : `https://gamma-api.polymarket.com/public-search?q=bitcoin&limit_per_type=20`.  
Slugs : `https://gamma-api.polymarket.com/events?slug={slug}` (pas `markets?slug=` pour events).  
Slug jour : `bitcoin-above-on-august-21-2026` **existe en event**. Interdit `q=crypto` (public-search : mars / Crypto: The Game). Interdit `events?order=volume24hr` (Dota/CS).  
Fed 11:08 : Hold **71.5%** (−1.0) hike25 **27.5%** (+1.0) 0 cuts **86.75%** hike2026 **49.5%** CLARITY **28.5%** (+5.0). CLOB last2=**0** vs fenêtre ≠0.

X MCP : `GetMcpTools server=X` → **not found**. `X_OAUTH_*` ≠ MCP.  
urllib `x.com/CryptoCavaleiro` = **200** len~253k, 0 `<time datetime>`, 5 status ids. Newest snowflake **10/08 14:54** = **J−10.84**. Giveaway string HTML oui ; browser visible = YT live 10/08 `#LiveTrading` + pin 02/04 RT @TeraHash clowns. **124** followers, **0 ticker**. xcancel = captcha. nitter = drop. CryptoPanic **403**.  
Browser 21/08 11:08 : wall lisible 2 posts puis login overlay. DROP_PRIMARY.

TV stilman50 0 ideas (`/published/` 404). Charts URL 200. Discord 0 env. DXY **98.646**. CoinDesk RSS 200 (Clarity ; 0 Nazarov/Garlinghouse). crypto.news **200 en 83ms**. Farside **403**. SEC RSS 200.

Persistance: daily **commit** `evals/last-daily-report.md`. 21/08 daily a **encore** refusé (« repo public ») après lecture du patch 20/08 ; improver persisté transcript `bc-0c68faf7`. Hop improver : fichier <36h OU `list-cloud-agents sources=automations` filter `8de46393` + batch-fetch OU INACCESSIBLE.
