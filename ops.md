# OPS — 2026-08-28 11:30 UTC

OHLC hop-1: `data-api.binance.vision` klines (~537–565 ms). `api.binance.com`/`fapi`=**451** **20 ms**. `api.binance.us` ETH 4h vol closed **44.14** vs Vision **31145** = **705.7×** ; live **23.58** vs **30590** = **1297.3×** (26/08 697×/997×) — **recalculer**. Interdit EMA/RSI US. TF=klines[:-1]. ONDO=`ONDOUSDT`. Funding/OI OKX BTC funding **0.0100 %** OI **28398**.

F&G: GET `https://api.alternative.me/fng/?limit=2` → **73 Greed** (J-1 **71**, 26/08 **65**). Daily 28/08 a fetch **limit=7** = FAIL.

CG: global cap **2.698 T** BTC.D **59.16**. `top_gainers_losers` 401. Tri client 250 vol≥5e6 : TRUMP +16.7 UB +13.2 ENA +12.4 MELANIA +8.8.

PM: `markets?q=` ignore q → Xi/Dem. Hop `public-search?q=bitcoin`. Hop-2 `events?slug=` ET `markets?slug=` (groupe **[]** + **child**). 80k Aug **HIT** (prices 1/0). Standalone `bitcoin-above-80k-in-august` **[]**. Ladder August : 82.5k **33.15** 85k **10.55**. Fed-762 Hold **68.5** hike25 **27.5** cut25 **1.65**. Slug court = **2025 closed**. 0-cuts nested **88.7 %**. YE `end-of-2026` [] → `what-price-will-bitcoin-hit-before-2027`.

CLOB: `clobTokenIds[0]`. urllib `interval=max` = **403 CF**. WebFetch **même URL** = 200. Hold n=744 last **68.5** last2 **0** / d24 **+1.0** ; 0-cuts n=721 last **88.7** last2 **0** / d24 **+1.05** ; 82.5k last **33.15** last2 **+2.3** / d24 **−10.1**. Daily 03h : startTs sans max, 0 last2, 0 markets?slug=, 0 clob_http_count.

X MCP: GetDynamicTools namespace=X → **needsAuth** tools=[]. `X_OAUTH_CLIENT_ID` présent ≠ MCP. urllib x.com=200 **html_n=255441** 0 datetime ; 5 IDs ; **1er ID = pin 02/04**. fxtwitter newest **27/08 09:03Z** `2092900767262757186` affiliate re.xyz giveaway=NON 0 ticker ; **122** fol / 1586 following. Daily : html 20 kB → 0 IDs, newest hérité 10/08, jina inventé. DROP_PRIMARY. CryptoPanic API **403**. Farside urllib 403 / daily 200. TFTC singulier 404 / pluriel 200.

TV stilman50 profil 200 ; `/published` 404 ; ideas API user= **ignoré** (The-Thief). DXY **99.173**. crypto.news 200 112 ms. Discord skip.

Persistance: daily 28/08 skip owner-only **encore**. 0 daily 27/08.
