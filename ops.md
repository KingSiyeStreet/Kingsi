# OPS — 2026-08-20

OHLC hop-1: `data-api.binance.vision/api/v3/klines` (~409ms). `fapi.binance.com`=451 42ms. `api.binance.us` ETH 4h vol **333** vs Vision **105076** = **315×** — interdit EMA. **TF = bougies closes `klines[:-1]`** ; live = ticker. Funding/OI OKX BTC funding **0.0100%** OI ~31158. Bybit 403.

F&G: GET `https://api.alternative.me/fng/?limit=2` → **62 Greed** (J-1 46). Interdit WebSearch.

CG: global cap **2.454T** BTC.D **58.74**. `order=percent_change_24h_desc` → BTC/ETH. `top_gainers_losers` 401. Tri client 250 vol≥5e6 : MET +25.1 HYPE +24.5 CASHCAT +24.4.

PM: default=5. **Interdit `q=crypto`** (20/08 encore : week of March 23, Fight Night vol 4.25M, Crypto: The Game 2024). Hop : `q=bitcoin` + slug `bitcoin-above-on-august-{j}-2026` (20/08 live, ≥54k ~99.95%). Interdit `events?order=volume24hr` (Dota 8.2M / LoL / Liga). Fed slugs : Hold **72.5%** hike25 **26.5%** ; 0 cuts **85.85%** ; hike2026 **49.5%**. CLOB last2=**0** vs fenêtre Hold **+1.0** / hike25 **−2.0** / hike2026 **−1.0**. Daily 20/08 a skip CLOB HTTP.

X MCP : `GetMcpTools server=X` → **not found**. `X_OAUTH_*` ≠ MCP.  
**urllib x.com/CryptoCavaleiro = 200** (plus 403). 5 articles, newest **10/08 = J-10**, giveaway **10/07**, pin 02/04 RT @TeraHash, **124** followers, **0 ticker**. HTML sans `<time datetime>` → dates via browser. xcancel = captcha. nitter = drop. CryptoPanic 404/403.  
Browser 20/08 : wall lisible sans login ; same newest 10/08.

TV stilman50 0 ideas (`/published/` 404). Charts URL 200. Discord 0 env. DXY **98.688**. CoinDesk RSS 200 (Clarity / 71–72k / 3 Md$ shorts). crypto.news/feed timeout 20s → **timeout 8s puis skip**.

Persistance: daily **commit** `evals/last-daily-report.md`. 20/08 daily a refusé (« repo public ») ; improver persisté transcript `bc-bd26f0c5`. Hop improver : fichier <36h OU `list-cloud-agents sources=automations` filter `8de46393` + batch-fetch OU INACCESSIBLE.
