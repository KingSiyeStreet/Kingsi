# Changelog — patches MÉTHODE (2026-08-23)

Déclenché par : persist 4e jour FAIL ; X=4 ; honnêteté=3 ; F&G limit=7 ; 0 CLOB HTTP ; slug Fed 2025 ; events/markets inversés.

1) Persist 4e jour (`bc-8ac6fc35`) : mémoire PUIS transcript, 0 fichier, 0 section Échecs. Phrases **interdites** comme motif de skip : « Repo Kingsi public », « transcript uniquement », « aucun entry/inval/TP commité ». Si le workspace n’a que `README.md` → `mkdir -p evals` et écrire quand même. `persist_commit` = 1re ligne d’Échecs. MEMORIES avant commit = FAIL.

2) X gated : 23/08 0 ping `GetMcpTools server=X`, label faux « non authentifié », 0 computerUse malgré 0 datetime/0 body, 0 champs gated. Interdit le mot « non authentifié » si `mcp_status≠needsAuth`. IDs snowflake ≠ skip hop-2. Giveaway = body newest (HTML 2 hits = FP ; newest = YT #Livetrading 10/08 ; giveaway = 10/07).

3) PM slug **les deux** endpoints : 23/08 `will-bitcoin-reach-80k-in-august-2026` events=[] markets=1 ; `fed-decision-in-september-762` events=1 markets=[]. Interdit de figer `markets?slug=` comme vide (patch 22/08 trop fort).

4) Fed slug court `fed-decision-in-september` (sans `-762`) = **Sept 2025 résolu** (prices 0/1). Résoudre via public-search titre 2026.

5) CLOB : daily 0 GET `prices-history`. Δ table = mémoire 22/08. Interdit. ≥3 HTTP CLOB ou section PM = échec. last2 Fed=0 **et** BTC80k last2 −1.0 / 24h −4.95 — ne pas figer last2=0.

6) F&G : daily encore `limit=7`. Premier GET = `limit=2` sinon `fng_limit=FAIL`.

7) ETF : interdit la ligne « ETF (Farside/TFTC) » si Farside HTTP=403. TFTC path 404 (ne pas figer URL). US minceur **356×** ce run (22/08 910×) — recalculer, ne pas figer. CryptoPanic 403 (22/08 404).

Coller `prompts/prompt-*.md` dashboard. Doublons `c490fbb7`/`8a20e9a7` not found.
