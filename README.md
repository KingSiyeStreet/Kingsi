# Kingsi

Le kit **bot crypto** (veille + contenu, pas de trading auto) est dans `bot-crypto/`.

Pour l’emporter dans un autre dépôt :

```bash
bash bot-crypto/copy-into.sh /chemin/vers/ton-nouveau-repo
```
